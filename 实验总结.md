# 实验总结：基于CLIP系列的文本图像匹配及其应用

## 1. 实验目标

本实验围绕 `实验指导.pdf` 中的要求，完成以下两个部分：

1. 文本图像匹配：
   - 读取 `dataset1/caption.json` 中的英文描述；
   - 将长 caption 压缩为更适合 CLIP 编码的视觉短句；
   - 使用 CLIP 提取文本和图像特征并完成检索；
   - 输出标准 JSON 结果并计算准确率。
2. 车牌识别扩展：
   - 读取固定尺寸车牌图像；
   - 切分 7 个字符区域；
   - 用 CLIP 图像特征与字符模板做相似度匹配；
   - 输出最终车牌号码并统计整牌精度。

实验环境使用 `myenv-3.9`，核心实现语言为 Python。

## 2. 项目当前实现内容

### 2.1 文本图像匹配模块

当前文本检索模块支持 4 种策略：

1. `raw`
   - 直接使用原始 caption 做 CLIP 检索。
2. `manual_compressed`
   - 使用人工规则压缩成长句视觉短语。
3. `deepseek_compressed`
   - 调用 DeepSeek API，将 caption 改写为更适合 CLIP 的视觉短句。
4. `ollama_compressed`
   - 调用本地 Ollama 服务，对 caption 做视觉短句压缩。

其中，默认和一键测试脚本使用的是 `manual_compressed`，而当前实验最优方案是 `deepseek_compressed`。

### 2.2 车牌识别模块

车牌识别模块基于固定字符分块和多特征模板匹配：

1. 按固定比例切出 7 个字符区域；
2. 使用白色前景掩膜进一步紧致裁剪；
3. 将待识别字符和模板字符都送入 CLIP 图像编码器，提取视觉特征；
4. 同时构造字符二值字形特征，补充笔画和轮廓信息；
5. 融合 CLIP 相似度与字形相似度进行匹配；
6. 对 `1/L` 等极易混淆字符加入定向判别；
7. 拼接得到最终车牌号。

### 2.3 Web 界面

项目提供 Flask 前端，统一入口为“基于CLIP系列的文本图像匹配及其应用”，并包含两个可切换模块：

1. 文本图像匹配模块
   - 输入文本；
   - 选择压缩策略；
   - 展示匹配到的图片；
   - 支持动态向图库中上传图片。
2. 车牌识别模块
   - 上传车牌图片；
   - 返回识别结果。

## 3. 核心算法思路

### 3.1 文本压缩与图像检索

CLIP 对超长 caption 的直接处理效果有限，因为长文本会引入大量抽象修辞、背景故事和情绪描述，这些内容对图像检索帮助不大，反而会稀释可见信息。

因此本实验采用“先压缩，再检索”的思路：

1. 从原始 caption 中保留主体、属性、动作、场景和空间关系；
2. 去掉抽象情绪、隐喻表达和无视觉贡献的修饰语；
3. 将压缩后的短句输入 CLIP 文本编码器；
4. 将图库图片预编码成图像特征；
5. 用余弦相似度选取最匹配图片。

在实现上，`manual_compressed` 倾向于保留连贯视觉短句，`deepseek_compressed` 与 `ollama_compressed` 使用不同 Prompt 单独优化，并通过缓存机制减少重复调用。

### 3.2 车牌识别

车牌识别不依赖 OCR 训练，而是直接将字符图块视为图像检索问题：

1. 依据固定模板框裁剪字符；
2. 利用白色字符区域收紧边界；
3. 用 CLIP 对字符图像编码，获得高层视觉语义特征；
4. 将字符转为归一化二值字形特征，增强对笔画形状的刻画；
5. 与模板库中的省份简称模板、字母数字模板分别做双路相似度匹配；
6. 对少量固定混淆对再做规则化精修；
7. 将 7 位字符拼接输出。

相比只使用 CLIP 特征的初版实现，这种“CLIP + 字形特征 + 轻量规则”的方式更适合本实验中固定版式、模板齐全的车牌数据。

## 4. 测试流程

### 4.1 一键测试

项目提供一键测试脚本：

```bash
~/AI-task1/run_task1_tests.sh
```

如果希望显式指定 Python 解释器，可执行：

```bash
PYTHON_BIN=python ~/AI-task1/run_task1_tests.sh
```

该脚本当前会依次完成：

1. `manual_compressed` 文本图像检索；
2. 文本检索结果准确率评估；
3. 车牌识别整批测试。

### 4.2 单独运行文本图像检索

1. 人工规则压缩：

```bash
python \
  ~/AI-task1/CLIP-pretrained-model/codes/text_image_retrieval.py \
  --device cpu \
  --strategy manual_compressed \
  --output_path ~/AI-task1/CLIP-pretrained-model/codes/result/caption_result_manual.json \
  --compression_output_path ~/AI-task1/CLIP-pretrained-model/codes/result/manual_compressions.json
```

2. DeepSeek 压缩：

```bash
python \
  ~/AI-task1/CLIP-pretrained-model/codes/text_image_retrieval.py \
  --device cpu \
  --strategy deepseek_compressed \
  --output_path ~/AI-task1/CLIP-pretrained-model/codes/result/caption_result_deepseek_v5.json \
  --compression_output_path ~/AI-task1/CLIP-pretrained-model/codes/result/deepseek_compressions_v5.json
```

3. Ollama 压缩：

```bash
python \
  ~/AI-task1/CLIP-pretrained-model/codes/text_image_retrieval.py \
  --device cpu \
  --strategy ollama_compressed \
  --output_path ~/AI-task1/CLIP-pretrained-model/codes/result/caption_result_ollama_v5.json \
  --compression_output_path ~/AI-task1/CLIP-pretrained-model/codes/result/ollama_compressions_v5.json
```

4. 原始文本对照组：

```bash
python \
  ~/AI-task1/CLIP-pretrained-model/codes/text_image_retrieval.py \
  --device cpu \
  --strategy raw \
  --output_path ~/AI-task1/CLIP-pretrained-model/codes/result/caption_result_raw.json \
  --compression_output_path ~/AI-task1/CLIP-pretrained-model/codes/result/raw_texts.json
```

### 4.3 评估文本检索准确率

```bash
cd ~/AI-task1/CLIP-pretrained-model/codes
python evaluate_result.py \
  --result_path ~/AI-task1/CLIP-pretrained-model/codes/result/caption_result_manual.json \
  --ground_truth_path ~/AI-task1/dataset1/caption_with_keywords_and_image.json
```

### 4.4 运行车牌识别

```bash
python \
  ~/AI-task1/CLIP-pretrained-model/codes/plate_recognition.py \
  --device cpu \
  --shape_weight 0.2 \
  --plate_dir ~/AI-task1/车牌识别/License_plate \
  --template_dir ~/AI-task1/车牌识别/Character_templates \
  --weight_path ~/AI-task1/CLIP-pretrained-model/codes/ViT-B-32.pt \
  --output_path ~/AI-task1/CLIP-pretrained-model/codes/result/plate_result.json \
  --evaluate_by_filename
```

### 4.5 启动前端界面

```bash
~/AI-task1/run_retrieval_ui.sh
```

浏览器访问：

```text
http://127.0.0.1:7860
```

## 5. 实测结果

### 5.1 文本图像匹配结果

在 `dataset1` 的 60 条 caption 上，当前各方案结果如下：

| 策略 | 正确数 | 准确率 |
| --- | --- | --- |
| `raw` | 38/60 | 63.33% |
| `manual_compressed` | 44/60 | 73.33% |
| `deepseek_compressed` | 54/60 | 90.00% |
| `ollama_compressed` | 44/60 | 73.33% |

其中最优结果文件为：

- `CLIP-pretrained-model/codes/result/caption_result_deepseek_v5.json`
- `CLIP-pretrained-model/codes/result/deepseek_compressions_v5.json`

### 5.2 车牌识别结果

在 `车牌识别/License_plate` 共 1010 张车牌图像上，本轮优化前后结果如下：

| 版本 | 方法概述 | 正确数 | 准确率 |
| --- | --- | --- | --- |
| 优化前 | 固定分块 + 白色区域紧致裁剪 + 纯 CLIP 模板匹配 | 785/1010 | 77.72% |
| 优化后 | 固定分块 + 白色区域紧致裁剪 + CLIP/字形特征融合 + `1/L` 定向判别 | 1010/1010 | 100.00% |

输出文件：

- `CLIP-pretrained-model/codes/result/plate_result.json`

## 6. 实验结果分析

### 6.1 文本图像匹配分析

当前结果说明，CLIP 检索效果与输入文本表达形式高度相关。

从实验结果看：

1. `raw` 最低，说明原始长句中的抽象叙事和修辞会干扰 CLIP 对可见信息的提取。
2. `manual_compressed` 明显优于 `raw`，说明“短而连贯的视觉短句”比原始长句更适合 CLIP。
3. `deepseek_compressed` 达到 `90.00%`，说明外部大模型可以更稳定地抽取主体、动作、属性和场景，是当前最佳方案。
4. `ollama_compressed` 与人工规则持平，说明本地小模型有可用性，但对 Prompt 更敏感，稳定性仍不如 DeepSeek。

### 6.2 车牌识别分析

车牌识别模块经过这轮优化后，整牌精确匹配率从 `77.72%` 提升到了 `100.00%`，说明在当前这套固定尺寸、固定颜色风格、模板完备的数据上，融合型模板匹配方案非常有效。

本次优化的关键改进点主要有：

1. 在原有 CLIP 相似度之外，增加了字符二值字形特征，补上了 CLIP 对细粒度笔画差异不够敏感的问题。
2. 将字符区域重新做前景紧致裁剪后，再提取字形轮廓，有效减少了蓝色背景和空白边缘的干扰。
3. 将 CLIP 特征与字形特征加权融合后，原来集中的 `7/2`、`F/E`、部分省份简称误判基本被消除。
4. 对最后残留的 `1/L` 混淆，使用字形宽高比进行定向判别，进一步补齐边界错误。

从逐位字符结果来看：

1. 省份简称位置识别率提升到 `100%`；
2. 第 3 到第 7 位字符识别率也提升到 `100%`；
3. 最后收敛到第二位字符的 `1/L` 固定混淆，再通过轻量规则彻底消除。

这也说明一个很重要的实验结论：对于结构规则、模板固定的字符识别任务，单纯依赖 CLIP 高层视觉特征还不够，加入明确的字形结构信息后，匹配精度会明显提高。

## 7. 当前交付内容

当前项目已经形成以下完整交付：

1. 文本图像检索主流程；
2. DeepSeek / Ollama / 人工规则 / 原始文本四组对比实验；
3. 中间压缩文本和最终检索结果 JSON；
4. 车牌识别扩展模块；
5. 双模块 Flask 前端；
6. 一键测试脚本；
7. 实验总结与 README 文档。

## 8. 结论

本实验已经完成“基于 CLIP 的文本图像匹配及其应用”的主要目标，并在原始实验要求基础上增加了：

1. 多种文本压缩策略对比；
2. DeepSeek API 与本地 Ollama 的接入；
3. 图形化前端交互；
4. 车牌识别扩展应用。

从当前结果看：

1. 最优文本检索方案为 `DeepSeek v5`，准确率 `90.00%`；
2. 人工规则和 Ollama 方案均达到 `73.33%`，可作为无需外部高质量 API 的备选；
3. 车牌识别方案在优化后达到 `100.00%`，验证了“CLIP + 字形特征 + 轻量规则”方案在该数据集上的有效性。
